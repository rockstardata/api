import { Repository } from 'typeorm';
import { CreateSaleDto } from './dto/create-sale.dto';
import { UpdateSaleDto } from './dto/update-sale.dto';
import { Sale, PaymentMethod, SaleStatus } from './entities/sale.entity';
import { Ticket } from 'src/tickets/entities/ticket.entity';
import { Venue } from 'src/venue/entities/venue.entity';
import { PermissionsService } from 'src/auth/permissions.service';
import { SyncService } from '../database/sync.service';
export declare class SalesService {
    private readonly saleRepository;
    private readonly ticketRepository;
    private readonly venueRepository;
    private readonly permissionsService;
    private readonly syncService;
    constructor(saleRepository: Repository<Sale>, ticketRepository: Repository<Ticket>, venueRepository: Repository<Venue>, permissionsService: PermissionsService, syncService: SyncService);
    create(createSaleDto: CreateSaleDto, userId?: number): Promise<Sale>;
    findAll(userId?: number): Promise<Sale[]>;
    findOne(id: number, userId?: number): Promise<Sale>;
    update(id: number, updateSaleDto: UpdateSaleDto, userId?: number): Promise<Sale>;
    remove(id: number, userId?: number): Promise<void>;
    findByVenue(venueId: number, userId?: number): Promise<Sale[]>;
    findByUser(userId: number): Promise<Sale[]>;
    getSalesSummary(venueId?: number, userId?: number): Promise<any>;
    getSalesByDateRange(startDate: Date, endDate: Date, venueId?: number, userId?: number): Promise<Sale[]>;
    getSalesByPaymentMethod(paymentMethod: PaymentMethod, venueId?: number, userId?: number): Promise<Sale[]>;
    getSalesByStatus(status: SaleStatus, venueId?: number, userId?: number): Promise<Sale[]>;
    private canAccessSale;
    private hasUpdatePermission;
    private hasDeletePermission;
}
